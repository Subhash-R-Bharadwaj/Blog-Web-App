<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="styles/main.css" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC"
      crossorigin="anonymous"
    />
    <title>Blog</title>
  </head>
  <body>
    <%- include("partials/header.ejs") %>

    <h1
      style="
        color: rgb(247, 250, 234);
        font-family: Cambria, Cochin, Georgia, Times, 'Times New Roman', serif;
      "
    >
      Hey. Create and edit your posts here!
    </h1>
    <hr />
    <form action="/form">
      <button type="/form" class="btn btn-warning">Create New Post</button>
    </form>
    <br />

    <div class="container">
      <div class="row row-cols-2">
        <div>
          <% if (locals?.title?.length > 0 && locals?.description?.length > 0)
          {%> <%for( var i = 0; i < locals?.title.length; i++){%>
          <div class="card" style="background-color: white;">
            <div class="card-title"><%=title[i]%></div>
            <div class="card-body"><%= description[i] %></div>
          </div>
          <% }%> <% } %>
        </div>
      </div>
    </div>

    <%- include("partials/footer.ejs") %>
  </body>
</html>
